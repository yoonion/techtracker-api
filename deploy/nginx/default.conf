upstream techtracker_api {
  server api:4000;
  keepalive 32;
}

server {
  listen 80;
  server_name api.techtracker.store;

  location /.well-known/acme-challenge/ {
    root /var/www/certbot;
  }

  location / {
    return 301 https://$host$request_uri;
  }
}

server {
  listen 443 ssl;
  http2 on;
  server_name api.techtracker.store;

  ssl_certificate /etc/letsencrypt/live/api.techtracker.store/fullchain.pem;
  ssl_certificate_key /etc/letsencrypt/live/api.techtracker.store/privkey.pem;

  client_max_body_size 10m;

  location / {
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header Connection "";
    proxy_pass http://techtracker_api;
    proxy_read_timeout 60s;
  }
}
